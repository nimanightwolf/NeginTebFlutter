

Future<void> lazyBootstrap() async {
// config , connect to database , get post from api, notification, check sms , sensor connection
// (1 , 2 seconds)
  // اتصال به دیتابیس
  // final appDatabase = AppDatabase();
  // final dio = Dio();
  //
  // // اتصال به API
  // final productRepository = ProductRepository(dio: dio, database: appDatabase);
  // final hotelRepository = HotelRepository(jsonDataService: JsonDataService());
  //
  // // انجام سایر تنظیمات
  // final widgetsBinding = WidgetsFlutterBinding.ensureInitialized();
  // FlutterNativeSplash.preserve(widgetsBinding: widgetsBinding);
  //
  // // سایر راه‌اندازی‌ها (مثلاً Notification, اتصال به سنسورها)
  // await Future.delayed(const Duration(seconds: 2)); // زمان‌بندی دلخواه
  //
  // // انجام راه‌اندازی نهایی
  // FlutterNativeSplash.remove();
}
